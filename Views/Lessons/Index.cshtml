@model Yoklama.Models.ViewModels.LessonsVm
@{
    ViewData["Title"] = "Dersler";
}

<div class="container-fluid lessons-page">
    <!-- PC Header - Modern Layout -->
    <div class="d-none d-md-flex justify-content-between align-items-center mb-4">
        <div class="d-flex align-items-center gap-3">
            <div>
                @if (Model.IsAdmin)
                {
                    <h2 class="mb-0 text-dark fw-bold">Tüm Dersler</h2>
                    <p class="text-muted mb-0 small">Sistemdeki tüm dersleri yönetin ve düzenleyin</p>
                }
                else
                {
                    <h2 class="mb-0 text-dark fw-bold">Derslerim</h2>
                    <p class="text-muted mb-0 small">Size atanmış dersleri görüntüleyin</p>
                }
            </div>
        </div>
        <div class="d-flex gap-2">
            <a asp-controller="Schedule" asp-action="Index" class="btn btn-outline-primary">
                <i class="fas fa-calendar-alt me-1"></i>Ders Programı
            </a>
            @if (Model.IsAdmin)
            {
                <a class="btn btn-primary" asp-action="CreateLesson">
                    <i class="fas fa-plus me-1"></i>Yeni Ders Ekle
                </a>
            }
        </div>
    </div>
    
    <!-- Mobile Header - Compact Layout -->
    <div class="d-md-none mb-3">
        <div class="d-flex align-items-center justify-content-between">
            <div class="d-flex align-items-center gap-2">
                <div class="icon-wrapper bg-primary text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 36px; height: 36px;">
                    <i class="fas fa-chalkboard-teacher"></i>
                </div>
                <div>
                    @if (Model.IsAdmin)
                    {
                        <h4 class="mb-0 text-dark fw-bold">Tüm Dersler</h4>
                    }
                    else
                    {
                        <h4 class="mb-0 text-dark fw-bold">Derslerim</h4>
                    }
                </div>
            </div>
            <div class="d-flex gap-1">
                <a asp-controller="Schedule" asp-action="Index" class="btn btn-outline-primary btn-md fs-5">
                    <i class="fas fa-calendar-alt"></i>
                </a>
                @if (Model.IsAdmin)
                {
                    <a class="btn btn-primary btn-sm" asp-action="CreateLesson">
                        <i class="fas fa-plus"></i>
                    </a>
                }
            </div>
        </div>
    </div>

    @if (Model.IsAdmin)
    {
        <div class="row mb-3">
            <div class="col-12">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <div class="row g-3 align-items-end">
                            @{ 
                                var adminGroups = Model.Lessons.Where(l => l.Group != null).Select(l => new { l.Group!.Id, l.Group!.Name }).Distinct().OrderBy(g => g.Name).ToList();
                                var adminTeachers = Model.Lessons.Where(l => l.Teacher != null).Select(l => new { l.Teacher!.Id, l.Teacher!.FullName }).Distinct().OrderBy(t => t.FullName).ToList();
                            }
                            <div class="col-md-3 col-sm-6">
                                <label for="adminGroupFilter" class="form-label fw-semibold"><i class="fas fa-layer-group me-2 text-primary"></i>Grup</label>
                                <select id="adminGroupFilter" class="form-select">
                                    <option value="">Tüm Gruplar</option>
                                    @foreach (var g in adminGroups)
                                    {
                                        <option value="@g.Name">@g.Name</option>
                                    }
                                </select>
                            </div>
                            <div class="col-md-3 col-sm-6">
                                <label for="adminTeacherFilter" class="form-label fw-semibold"><i class="fas fa-chalkboard-teacher me-2 text-primary"></i>Öğretmen</label>
                                <select id="adminTeacherFilter" class="form-select">
                                    <option value="">Tüm Öğretmenler</option>
                                    @foreach (var t in adminTeachers)
                                    {
                                        <option value="@t.FullName">@t.FullName</option>
                                    }
                                </select>
                            </div>
                            <div class="col-md-6">
                                <label for="adminLessonSearch" class="form-label fw-semibold"><i class="fas fa-book me-2 text-primary"></i>Ders Ara</label>
                                <div class="input-group">
                                    <input id="adminLessonSearch" type="text" class="form-control" placeholder="Ders başlığı yazın...">
                                    <button id="adminLessonSearchBtn" class="btn btn-primary" type="button"><i class="fas fa-search"></i></button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }

    @{ var teacherDistinctLessons = Model.IsAdmin ? Model.Lessons : Model.Lessons.GroupBy(l => l.Title).Select(g => g.First()).ToList(); }
    @if ((Model.IsAdmin ? Model.Lessons : teacherDistinctLessons).Any())
    {
        <div class="row">
            <div class="col-12">
                <div class="card border-0 shadow-sm desktop-table">
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th class="border-0 py-3 px-4">
                                            <i class="fas fa-book me-2 text-primary"></i>Ders
                                        </th>
                                        @if (Model.IsAdmin)
                                        {
                                            <th class="border-0 py-3 px-4">
                                                <i class="fas fa-calendar-day me-2 text-primary"></i>Gün/Saat
                                            </th>
                                            <th class="border-0 py-3 px-4">
                                                <i class="fas fa-layer-group me-2 text-primary"></i>Grup
                                            </th>
                                            <th class="border-0 py-3 px-4">
                                                <i class="fas fa-chalkboard-teacher me-2 text-primary"></i>Öğretmen
                                            </th>
                                            <th class="border-0 py-3 px-4 text-center">
                                                <i class="fas fa-cogs me-2 text-primary"></i>İşlemler
                                            </th>
                                        }
                                        else
                                        {
                                            <th class="border-0 py-3 px-4">
                                                <i class="fas fa-layer-group me-2 text-primary"></i>Grup
                                            </th>
                                            <th class="border-0 py-3 px-4">
                                                <i class="fas fa-hourglass-half me-2 text-primary"></i>Süre
                                            </th>
                                        }
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var lesson in (Model.IsAdmin ? Model.Lessons : teacherDistinctLessons))
                                    {
                                        <tr class="border-bottom" data-group="@(lesson.Group?.Name?.ToLower() ?? "")" data-teacher="@(lesson.Teacher?.FullName?.ToLower() ?? "")" data-title="@lesson.Title.ToLower()">
                                            <td class="py-3 px-4">
                                                <div class="d-flex align-items-center">
                                                    <div class="bg-primary bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                        <i class="fas fa-book text-primary"></i>
                                                    </div>
                                                    <div class="flex-grow-1">
                                                        <div class="fw-semibold text-truncate" style="max-width: 220px;" title="@lesson.Title">@lesson.Title</div>
                                                    </div>
                                                </div>
                                            </td>
                                            @if (Model.IsAdmin)
                                            {
                                                <td class="py-3 px-4">
                                                    <small class="text-muted">@GetDayName(lesson.DayOfWeek) @($"{lesson.StartTime.Hours:D2}:{lesson.StartTime.Minutes:D2}")</small>
                                                </td>
                                                <td class="py-3 px-4">
                                                    <small class="text-muted">@(lesson.Group?.Name ?? "Grup Yok")</small>
                                                </td>
                                                <td class="py-3 px-4">
                                                    <small class="text-muted">@(lesson.Teacher?.FullName ?? "Öğretmen Yok")</small>
                                                </td>
                                                <td class="py-3 px-4 text-end">
                                                    <div class="d-flex gap-2 justify-content-end align-items-center">
                                                        <button type="button" class="btn btn-sm" style="background: #f8fafc; border: 1px solid #e2e8f0; color: #475569; border-radius: 6px; font-weight: 500;" 
                                                                onclick="showLessonDetails('@lesson.Id', '@lesson.Title', '@(lesson.Teacher?.FullName ?? "Öğretmen Yok")', '@(lesson.Group?.Name ?? "Grup Yok")', '@GetDayName(lesson.DayOfWeek)', '@lesson.StartTime.ToString(@"hh\:mm")', '@lesson.EndTime.ToString(@"hh\:mm")', '@(lesson.SessionId?.ToString() ?? "")')">
                                                            <i class="fas fa-info-circle"></i> Detaylar
                                                        </button>
                                                    </div>
                                                </td>
                                            }
                                            else
                                            {
                                                <td class="py-3 px-4">
                                                    <small class="text-muted">@(lesson.Group?.Name ?? "Grup Yok")</small>
                                                </td>
                                                <td class="py-3 px-4">
                                                    <small class="text-muted">@((lesson.EndTime - lesson.StartTime).TotalMinutes) dk</small>
                                                </td>
                                            }
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Mobil Görünüm - Kartlar -->
        <div class="mobile-cards" style="display: none;">
            @foreach (var lesson in (Model.IsAdmin ? Model.Lessons : teacherDistinctLessons))
            {
                <div class="mobile-lesson-card">
                    <div class="mobile-lesson-header">
                        <h5 class="mobile-lesson-title">@lesson.Title</h5>
                        @if (Model.IsAdmin)
                        {
                            <div class="mobile-lesson-time">
                                @GetDayName(lesson.DayOfWeek) @lesson.StartTime.ToString(@"hh\:mm") - @lesson.EndTime.ToString(@"hh\:mm")
                            </div>
                        }
                        else
                        {
                            <div class="mobile-lesson-time">@GetDayName(lesson.DayOfWeek)</div>
                        }
                    </div>
                    
                    @if (Model.IsAdmin)
                    {
                        <div class="mobile-lesson-info">
                            <div class="mobile-lesson-info-item">
                                <i class="fas fa-user"></i>
                                <span>@(lesson.Teacher?.FullName ?? "Öğretmen Yok")</span>
                            </div>
                            <div class="mobile-lesson-info-item">
                                <i class="fas fa-layer-group"></i>
                                <span>@(lesson.Group?.Name ?? "Grup Yok")</span>
                            </div>
                            <div class="mobile-lesson-info-item">
                                <i class="fas fa-info-circle"></i>
                                <span class="mobile-session-status @GetSessionStatusClass(lesson.SessionStatus)">
                                    @GetSessionStatusText(lesson.SessionStatus)
                                </span>
                            </div>
                        </div>
                    }
                    else
                    {
                        <div class="mobile-lesson-info">
                            <div class="mobile-lesson-info-item"><i class="fas fa-layer-group"></i><span>@(lesson.Group?.Name ?? "Grup Yok")</span></div>
                        </div>
                    }
                    
                    @if (Model.IsAdmin)
                    {
                        <div class="mobile-lesson-actions">
                            <button type="button" class="mobile-btn mobile-btn-outline-info" 
                                    onclick="showLessonDetailsWithActions('@lesson.Id', '@lesson.Title', '@(lesson.Teacher?.FullName ?? "Öğretmen Yok")', '@(lesson.Group?.Name ?? "Grup Yok")', '@GetDayName(lesson.DayOfWeek)', '@lesson.StartTime.ToString(@"hh\:mm")', '@lesson.EndTime.ToString(@"hh\:mm")', '@(lesson.SessionId?.ToString() ?? "")')">
                                <i class="fas fa-info-circle me-1"></i>Detaylar
                            </button>
                        </div>
                    }
                </div>
            }
        </div>
    }
    else
    {
        <div class="row">
            <div class="col-12">
                <div class="card border-0 shadow-sm">
                    <div class="card-body text-center py-5">
                        <i class="fas fa-book fa-3x text-muted mb-3"></i>
                        <h4 class="text-muted mb-3">Henüz Ders Yok</h4>
                        <p class="text-muted mb-4">
                            @if (Model.IsAdmin)
                            {
                                <text>Sistemde henüz ders bulunmuyor. Yeni ders eklemek için yukarıdaki butonu kullanın.</text>
                            }
                            else
                            {
                                <text>Size henüz ders atanmamış. Ders programınızı kontrol etmek için aşağıdaki butonu kullanın.</text>
                            }
                        </p>
                        <div class="d-flex gap-2 justify-content-center">
                            @if (Model.IsAdmin)
                            {
                                <a class="btn btn-primary" asp-action="CreateLesson">
                                    <i class="fas fa-plus me-1"></i>Yeni Ders Ekle
                                </a>
                            }
                            <a asp-controller="Schedule" asp-action="Index" class="btn btn-outline-primary">
                                <i class="fas fa-calendar-alt me-1"></i>Ders Programı
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

@functions {
    bool IsToday(int dayOfWeek)
    {
        var today = DateTime.Today.DayOfWeek; // Sunday=0
        var todayIndex = today == DayOfWeek.Sunday ? 7 : (int)today; // Monday=1..Sunday=7
        return dayOfWeek == todayIndex;
    }
    string GetDayName(int dayOfWeek)
    {
        return dayOfWeek switch
        {
            1 => "Pazartesi",
            2 => "Salı",
            3 => "Çarşamba",
            4 => "Perşembe",
            5 => "Cuma",
            6 => "Cumartesi",
            7 => "Pazar",
            _ => "Bilinmeyen"
        };
    }
    
    string GetSessionStatusClass(Yoklama.Models.Entities.SessionStatus? status)
    {
        return status switch
        {
            Yoklama.Models.Entities.SessionStatus.Open => "mobile-session-open",
            Yoklama.Models.Entities.SessionStatus.Closed => "mobile-session-closed",
            Yoklama.Models.Entities.SessionStatus.Finalized => "mobile-session-closed",
            _ => "mobile-session-none"
        };
    }
    
    string GetSessionStatusText(Yoklama.Models.Entities.SessionStatus? status)
    {
        return status switch
        {
            Yoklama.Models.Entities.SessionStatus.Open => "Bekliyor",
            Yoklama.Models.Entities.SessionStatus.Closed => "Kapatıldı",
            Yoklama.Models.Entities.SessionStatus.Finalized => "Tamamlandı",
            _ => "Durum Yok"
        };
    }
}

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Ders Sil</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p id="deleteMessage">Dersini silmek istediğinizden emin misiniz?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
                <form id="deleteForm" method="post" style="display: inline;">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger">Sil</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function deleteLesson(button) {
            var id = button.getAttribute('data-lesson-id');
            var title = button.getAttribute('data-lesson-title');
            
            // Modal mesajını güncelle
            var messageElement = document.getElementById('deleteMessage');
            if (messageElement) {
                messageElement.textContent = '"' + title + '" dersini silmek istediğinizden emin misiniz?';
            }
            
            // Form action'ını ayarla
            document.getElementById('deleteForm').action = '@Url.Action("DeleteLesson")' + '?id=' + id;
            
            // Modal'ı göster
            new bootstrap.Modal(document.getElementById('deleteModal')).show();
        }
        
        function deleteLessonFromModal(id, title) {
            // Mevcut detay modalını kapat
            var detailModal = document.querySelector('.modal.show');
            if (detailModal) {
                var bsModal = bootstrap.Modal.getInstance(detailModal);
                if (bsModal) {
                    bsModal.hide();
                }
            }
            
            // Modal mesajını güncelle
            var messageElement = document.getElementById('deleteMessage');
            if (messageElement) {
                messageElement.textContent = '"' + title + '" dersini silmek istediğinizden emin misiniz?';
            }
            
            // Form action'ını ayarla
            document.getElementById('deleteForm').action = '@Url.Action("DeleteLesson")' + '?id=' + id;
            
            // Delete modal'ını göster
            setTimeout(function() {
                new bootstrap.Modal(document.getElementById('deleteModal')).show();
            }, 300);
        }
        
        // Admin için ders detayları ve işlemler gösterme fonksiyonu (sadece admin kullanabilir)
        function showLessonDetailsWithActions(id, title, teacher, group, day, startTime, endTime, sessionId) {
            var modal = document.createElement('div');
            modal.className = 'modal fade';
            modal.innerHTML = `
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                        <div class="modal-header bg-primary text-white">
                            <h5 class="modal-title mb-0">
                                <i class="fas fa-info-circle me-2"></i>Ders Detayları
                            </h5>
                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body p-3 p-md-4">
                            <div class="row g-3 g-md-4">
                                <div class="col-12">
                                    <div class="card border-0 shadow-sm">
                                        <div class="card-header bg-light border-0">
                                            <h6 class="card-title text-primary mb-0">
                                                <i class="fas fa-book me-2"></i>${title}
                                            </h6>
                                        </div>
                                        <div class="card-body p-3 p-md-4">
                                            <div class="row g-3">
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-success bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-user text-success"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Öğretmen</small>
                                                            <span class="fw-semibold text-dark">${teacher}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-info bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-users text-info"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Grup</small>
                                                            <span class="fw-semibold text-dark">${group}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-warning bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-calendar-day text-warning"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Gün</small>
                                                            <span class="fw-semibold text-dark">${day}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-danger bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-clock text-danger"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Saat Aralığı</small>
                                                            <span class="fw-semibold text-dark">${startTime} - ${endTime}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="modal-footer bg-light border-0 p-3">
                            <div class="row g-2 w-100">
                                <div class="col-6">
                                    <a href="/Lessons/EditLesson?id=${id}" class="btn btn-sm w-100" style="background: #3b82f6; border: 1px solid #3b82f6; color: white; border-radius: 6px; font-weight: 500;">
                                        <i class="fas fa-edit me-1"></i>Güncelle
                                    </a>
                                </div>
                                <div class="col-6">
                                    <button type="button" class="btn btn-sm w-100" style="background: #ef4444; border: 1px solid #ef4444; color: white; border-radius: 6px; font-weight: 500;" onclick="deleteLessonFromModal('${id}', '${title}')">
                                        <i class="fas fa-trash me-1"></i>Sil
                                    </button>
                                </div>
                                <div class="col-6">
                                    <button type="button" class="btn btn-sm w-100" style="background: #f8fafc; border: 1px solid #e2e8f0; color: #475569; border-radius: 6px; font-weight: 500;" data-bs-dismiss="modal">Kapat</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            `;
            
            document.body.appendChild(modal);
            var bsModal = new bootstrap.Modal(modal);
            bsModal.show();
            
            modal.addEventListener('hidden.bs.modal', function() {
                document.body.removeChild(modal);
            });
        }
        
        // Admin için ders detayları gösterme fonksiyonu
        function showLessonDetails(id, title, teacher, group, day, startTime, endTime, sessionId) {
            var modal = document.createElement('div');
            modal.className = 'modal fade';
            modal.innerHTML = `
                <div class="modal-dialog modal-lg">
                    <div class="modal-content">
                        <div class="modal-header bg-primary text-white">
                            <h5 class="modal-title mb-0">
                                <i class="fas fa-info-circle me-2"></i>Ders Detayları
                            </h5>
                            <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                        </div>
                        <div class="modal-body p-3 p-md-4">
                            <div class="row g-3 g-md-4">
                                <div class="col-12">
                                    <div class="card border-0 shadow-sm">
                                        <div class="card-header bg-light border-0">
                                            <h6 class="card-title text-primary mb-0">
                                                <i class="fas fa-book me-2"></i>${title}
                                            </h6>
                                        </div>
                                        <div class="card-body p-3 p-md-4">
                                            <div class="row g-3">
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-success bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-user text-success"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Öğretmen</small>
                                                            <span class="fw-semibold text-dark">${teacher}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-info bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-users text-info"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Grup</small>
                                                            <span class="fw-semibold text-dark">${group}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-warning bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-calendar-day text-warning"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Gün</small>
                                                            <span class="fw-semibold text-dark">${day}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-12 col-md-6">
                                                    <div class="d-flex align-items-center">
                                                        <div class="bg-danger bg-opacity-10 rounded-circle p-2 me-3 d-flex align-items-center justify-content-center" style="width: 40px; height: 40px;">
                                                            <i class="fas fa-clock text-danger"></i>
                                                        </div>
                                                        <div>
                                                            <small class="text-muted d-block">Saat Aralığı</small>
                                                            <span class="fw-semibold text-dark">${startTime} - ${endTime}</span>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="modal-footer bg-light border-0 p-3">
                            <div class="row g-2 w-100">
                                <div class="col-6">
                                    <a href="/Lessons/EditLesson?id=${id}" class="btn btn-sm w-100" style="background: #3b82f6; border: 1px solid #3b82f6; color: white; border-radius: 6px; font-weight: 500;">
                                        <i class="fas fa-edit me-1"></i>Güncelle
                                    </a>
                                </div>
                                <div class="col-6">
                                    <button type="button" class="btn btn-sm w-100" style="background: #ef4444; border: 1px solid #ef4444; color: white; border-radius: 6px; font-weight: 500;" onclick="deleteLessonFromModal('${id}', '${title}')">
                                        <i class="fas fa-trash me-1"></i>Sil
                                    </button>
                                </div>
                                <div class="col-6">
                                    <button type="button" class="btn btn-sm w-100" style="background: #f8fafc; border: 1px solid #e2e8f0; color: #475569; border-radius: 6px; font-weight: 500;" data-bs-dismiss="modal">Kapat</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            `;
            
            document.body.appendChild(modal);
            var bsModal = new bootstrap.Modal(modal);
            bsModal.show();
            
            // Modal kapandığında DOM'dan kaldır
            modal.addEventListener('hidden.bs.modal', function() {
                document.body.removeChild(modal);
            });
        }
        
        (function () {
            var groupEl = document.getElementById('adminGroupFilter');
            var teacherEl = document.getElementById('adminTeacherFilter');
            var searchEl = document.getElementById('adminLessonSearch');
            var searchBtn = document.getElementById('adminLessonSearchBtn');

            function applyFilters() {
                var g = (groupEl && groupEl.value || '').toLowerCase();
                var t = (teacherEl && teacherEl.value || '').toLowerCase();
                var q = (searchEl && searchEl.value || '').toLowerCase().trim();

                var rows = document.querySelectorAll('table tbody tr');
                rows.forEach(function (row) {
                    var rg = (row.getAttribute('data-group') || '').toLowerCase();
                    var rt = (row.getAttribute('data-teacher') || '').toLowerCase();
                    var rv = (row.getAttribute('data-title') || '').toLowerCase();

                    var ok = true;
                    if (g && rg !== g) ok = false;
                    if (t && rt !== t) ok = false;
                    if (q && rv.indexOf(q) === -1) ok = false;
                    row.style.display = ok ? '' : 'none';
                });
            }

            if (groupEl) groupEl.addEventListener('change', applyFilters);
            if (teacherEl) teacherEl.addEventListener('change', applyFilters);
            if (searchBtn) searchBtn.addEventListener('click', applyFilters);
            if (searchEl) searchEl.addEventListener('keypress', function (e) { if (e.key === 'Enter') { e.preventDefault(); applyFilters(); } });
        })();

    </script>
}
